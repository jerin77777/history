# history

install packages using:
pip install -r requirements.txt

add this line to main.py
os.environ["SAMBA_KEY"] = "YOUR KEY"

run the app using:
python main.py

access the app with url:
http://localhost:5000/
